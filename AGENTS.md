# 统计套利论文复刻项目：AI 协作规范

最后更新：2026-07-17  
适用范围：本目录及所有子目录

## 1. 项目目标

本项目将分阶段复刻 Cartea、Cucuringu 与 Jin 的论文 *Correlation Matrix Clustering for Statistical Arbitrage Portfolios* 所描述的统计套利程序。

用户会逐步给出每个阶段的具体任务、输入、输出和要求，例如数据处理、构建 clusters、选择股票、构建 portfolio 和回测。AI 必须与用户逐阶段讨论和实现，不能现在就替后续模块确定具体设计，也不能未经要求一次性完成整个系统。

当前尚未确定编程语言、数据来源、文件格式、schema、类接口、目录结构或各模块的具体算法细节。这些内容必须在对应阶段到来时再与用户确认。

## 2. 需要提前了解的背景

### 2.1 主要资料

- 主论文：`Correlation_Matrix_Clustering_for_Statistical_Arbitrage_Portfolios_CarteaAlvaro_JinQi_CucuringuMihai.pdf`
- 用户笔记：`Markdown_Notes.zip` 中的 Markdown 文件。
- 作者聚类示例：<https://github.com/maxclchen/Correlation-Matrix-Clustering-for-Statistical-Arbitrage-Portfolios>
- 次级参考：本目录中的扩展论文和 PPT。

主论文是方法基线；用户笔记帮助理解目标，但可能包含简化、疑问或笔误；作者代码只覆盖部分聚类示例，不是完整策略实现；扩展论文中的机器学习、Kelly sizing、交易成本优化等只能作为后续改进参考。

### 2.2 信息优先级

发生冲突时按以下顺序处理：

1. 用户当前的明确要求。
2. 用户在先前阶段已经确认的决定。
3. 主论文。
4. 用户笔记和作者示例代码。
5. 次级资料和 AI 的一般建议。

AI 不得静默解决冲突。必须指出不同来源的差异、解释影响，并与用户确认采用哪一种解释。

### 2.3 论文方法概览

论文的整体思路是：先用市场残差收益构建股票相关矩阵，再使用图聚类算法把股票划分为互不重叠的 clusters；随后在每个 cluster 内寻找相对 cluster 平均收益的 previous winners 和 previous losers，做空 winners、做多 losers，并通过滚动更新和再平衡构建统计套利策略。

论文涉及的主要阶段包括：

1. 数据获取、清洗和股票池构建。
2. 收益、beta、市场残差收益和相关矩阵。
3. cluster 数量选择和图聚类。
4. winner/loser 识别。
5. portfolio 权重和再平衡。
6. 回测、基准和绩效评价。

论文中的 60 日 beta 窗口、5 日观察窗口、3 日再平衡、固定 `K=30`、动态 K、阈值等属于需要复刻和核对的论文设定，不代表工程实现方式已经确定。

## 3. 分阶段协作规则

每个阶段开始时，AI 应先与用户对齐：

- 本阶段的目标、范围和非目标；
- 对应的论文或笔记内容；
- 输入和输出的含义、格式与示例；
- 时间窗口、时点和数据可用性；
- 论文没有说明或资料之间冲突的地方；
- 本阶段需要的最小代码结构；
- 验收方式和必要测试；
- 可以考虑但不会自动加入的改进建议。

如果这些内容已经由用户完整说明，AI 可以复述关键约束后开始实现。如果仍存在会明显改变结果的歧义，应先给出推荐方案并等待确认。

不要为了等待所有未来需求而阻塞当前阶段。只确认完成当前阶段所必需的事项。

## 4. 编码与设计原则

代码应遵循 OOP、SRP、OCP、LSP、ISP、DIP、DRY 和 KISS，但必须务实应用：

- 每个组件只承担清晰、单一的职责。
- 数据获取、数学计算、信号、组合、回测和输出应保持合理分离。
- 需要替换的算法或外部依赖应具有稳定边界。
- 上层流程不应绑定某个数据供应商、文件格式或具体算法库。
- 公共计算规则只保留一个权威实现。
- 优先使用组合，避免深继承和大型万能类。
- 不为尚未出现的需求提前创建复杂抽象。
- 先实现清晰、正确、可验证的论文基线，再讨论性能优化或功能扩展。

具体使用哪些类、接口、数据对象和设计模式，必须根据阶段需求决定，而不是由本文档预先规定。

## 5. 正确性与研究规范

- 所有计算必须明确时间语义，避免 look-ahead bias。
- 股票池和历史成分必须注意 survivorship bias。
- 论文没有说明的数据清洗、缺失值、成交、成本和资金规则不能被隐藏为默认假设。
- 随机算法应可复现，随机种子和关键配置应能够记录。
- 每个阶段都应有与风险相称的测试，并能用小型样例核对关键中间结果。
- 不把 notebook 作为生产逻辑的唯一来源。
- 无法精确复现论文结果时，应先比较数据、参数和中间产物，再解释差异，不能只根据最终收益判断代码是否正确。

## 6. 论文基线与改进必须分离

项目首先复刻论文基线。任何改进都必须先与用户沟通，并说明：

- 它解决什么问题；
- 是否偏离论文；
- 可能带来的收益、风险和复杂度；
- 如何设计对照实验和判断是否有效。

交易成本、滑点、协方差收缩、风险约束、机器学习过滤器、Kelly sizing 等均属于可能的后续研究方向，不得静默混入基线。

## 7. 与用户沟通的要求

- 默认使用中文解释，代码标识符使用清晰的英文。
- 区分“论文明确规定”“用户已确认”“AI 建议”和“尚待确认”。
- 不只汇报代码完成，还要解释输入、输出、算法含义、边界情况、测试结果和已知限制。
- 发现可改进之处时主动提出，但未经确认不实施会改变策略含义的改进。
- 用户提出新要求后，应判断它是补充当前阶段还是替换原要求，并及时调整。

## 8. AI 不应做的事情

- 不提前设计或实现尚未讨论的后续模块。
- 不把论文参数、笔记示例或 AI 建议自动变成最终接口。
- 不在有实质影响的歧义上自行做决定。
- 不把改进版本的结果当作论文基线结果。
- 不为了形式上的 OOP 或 SOLID 过度工程化。
- 不省略会影响用户理解、复现或验证结果的重要信息。

后续始终遵循：当前阶段先沟通清楚，再实现和验证；完成后向用户解释结果与限制，然后进入下一阶段。
